## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app locally:
   `npm run dev`

The app uses Next.js and can be accessed at `http://localhost:3000`.
"# mai-task" 
